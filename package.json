{
  "name": "bittensor-intel",
  "private": true,
  "scripts": {
    "start:dev": "concurrently -n backend,frontend -c blue,green 'cd frontend && npm run dev' 'uvicorn src.api.main:app --reload --host 0.0.0.0 --port 8000'",
    "start:prod": "npm run build:frontend && uvicorn src.api.main:app --host 0.0.0.0 --port 8000",
    "build:frontend": "cd frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
